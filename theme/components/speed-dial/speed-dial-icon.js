import React from "react";
import { Plus } from "../../../icons/line/navigation";

export const speedDialIcon = {
  defaultProps: {
    icon: React.cloneElement(Plus),
  },
};

export default speedDialIcon;
