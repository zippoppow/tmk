import { shape } from "../styles";

export const appBar = {
  styleOverrides: {
    positionRelative: {
      borderRadius: shape.borderRadiusLG
    },
    positionSticky: {
      borderRadius: shape.borderRadiusLG
    },
    positionStatic: {
      borderRadius: shape.borderRadiusLG
    }
  }
};

export default appBar;
