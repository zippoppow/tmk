import { typography as baseTypography } from "../styles";

export const typography = {
  styleOverrides: {
    root: {
      fontFamily: baseTypography.fontFamily
    }
  }
};

export default typography;
