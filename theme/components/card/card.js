import { shape } from "../../styles";

export const card = {
  styleOverrides: {
    root: {
      borderRadius: shape.borderRadiusLG,
      boxShadow: "none"
    },
    paper: {
      boxShadow: "none"
    }
  }
};

export default card;
